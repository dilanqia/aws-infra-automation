# AWS Infrastructure Automation

Python scripts & CloudFormation templates for AWS provisioning.

## Components
- VPC & networking setup
- ECS cluster provisioning
- RDS database creation
- S3 bucket policies
- IAM role management

## Run
```bash
pip install -r requirements.txt
python provision.py --env staging --region us-east-1
```
